# Literacy Level Assessment System - Environment Configuration
# Copy this file to examples/literacy-assessment/.env and customize as needed

# ============================================================================
# AWS Configuration
# ============================================================================

# AWS Region for Bedrock Knowledge Bases
AWS_REGION=eu-central-1

# AWS Profile (configured in ~/.aws/credentials)
AWS_PROFILE=mll-dev

# ============================================================================
# AWS Bedrock Knowledge Base IDs
# ============================================================================
# Each literacy level has a dedicated knowledge base with curriculum content

# Level 1: Foundational literacy content
KB_LEVEL_1_ID=QADZTSAPWX

# Level 2: Intermediate literacy content
KB_LEVEL_2_ID=KGGD2PTQ2N

# Level 3: Advanced literacy content
KB_LEVEL_3_ID=7MGFSODDVI

# Level 4: Expert literacy content (currently shares KB with Level 3)
KB_LEVEL_4_ID=7MGFSODDVI

# ============================================================================
# LLM Configuration (Optional - AWS Bedrock)
# ============================================================================

# AWS Bedrock Model ID - EU region models (default: Claude Sonnet 4.5)
# LLM_MODEL_ID=eu.anthropic.claude-sonnet-4-5-20250929-v1:0

# Fast model for simple/quick tasks (default: Claude Haiku 4.5)
# LLM_FAST_MODEL_ID=eu.anthropic.claude-haiku-4-5-20251001-v1:0

# LLM Temperature (0-1, default: 0.7)
# LLM_TEMPERATURE=0.7

# Maximum tokens for LLM response (default: 4096)
# LLM_MAX_TOKENS=4096

# ============================================================================
# Assessment Configuration (Optional Overrides)
# ============================================================================

# Maximum time in seconds for single assessment generation (default: 60)
# ASSESSMENT_TIMEOUT=60

# Number of results to retrieve from knowledge base per query (default: 10)
# KB_MAX_RESULTS=10

# Minimum number of modules to cover per assessment (default: 5)
# MIN_MODULES_COVERED=5

# ============================================================================
# S3 Upload Configuration (Optional)
# ============================================================================

# S3 bucket for storing generated assessments
# Uses the same bucket as knowledge base content for simplified IAM permissions
S3_BUCKET_NAME=literacy-framework-development-961105418118-eu-central-1

# S3 prefix (path) for assessment files
# Assessments will be uploaded to: s3://{bucket}/{prefix}/level_{level}/
S3_PREFIX=learning_path/assessments

# Enable/disable S3 upload (true/false)
# Set to false for local testing without S3 access
ENABLE_S3_UPLOAD=true

# S3 region (inherits from AWS_REGION by default)
# Uncomment to override AWS_REGION for S3 operations
# S3_REGION=eu-central-1

# ============================================================================
# Notes
# ============================================================================
#
# AWS Credentials:
# - Credentials are loaded from AWS profile 'mll-dev' in ~/.aws/credentials
# - Ensure the profile has permissions for:
#   - bedrock-agent-runtime:Retrieve (for Knowledge Base queries)
#   - bedrock-agent-runtime:RetrieveAndGenerate (for Knowledge Base queries)
#   - bedrock:InvokeModel (for ChatBedrock LLM calls)
#   - s3:PutObject (for uploading assessments to S3)
#   - s3:GetObject (for reading uploaded assessments)
#   - s3:ListBucket (for listing assessments in S3)
#   - s3:HeadBucket (for validating S3 bucket access)
#
# Knowledge Bases:
# - All KBs are in eu-central-1 region
# - Level 3 and Level 4 currently share the same knowledge base
# - Each KB contains curriculum content organized by modules/courses
#
# Models:
# - Using EU-specific model IDs (eu.anthropic.*)
# - Claude Sonnet 4.5: Default model for all assessments (best quality)
# - Claude Haiku 4.5: Available for faster/simpler tasks (faster, lower cost)
# - Note: Claude 3.5 Sonnet will be deprecated, migrated to 4.5
#
# Security:
# - NEVER commit .env files to version control
# - .gitignore should exclude .env files
# - Store sensitive credentials securely
#
